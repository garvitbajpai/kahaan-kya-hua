{
  "name": "kahaan-kya-hua",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "node --v8-pool-size=1 --max-old-space-size=256 server.js",
    "dev": "node server.js",
    "build": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.6.1",
    "ejs": "^3.1.10",
    "express": "^4.22.1",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
